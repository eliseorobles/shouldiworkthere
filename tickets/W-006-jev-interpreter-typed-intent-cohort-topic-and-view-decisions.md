---
id: W-006
title: Jev interpreter: typed intent, cohort, topic, and view decisions
phase: Phase 2 - Inference
status: DONE
depends_on: W-001
completed: 2026-09-22 21:11 UTC
---

# W-006 - Jev interpreter: typed intent, cohort, topic, and view decisions

**Phase:** Phase 2 - Inference  
**Depends on:** W-001  
**Status:** DONE

## Scope

Server-side TypeSafe integration with parallel Noul/Choice/Score questions, closed-set company selection, clarification detection, confidence gating, strict validation, timeout and fallback to deterministic controls, model/prompt versioning.

## Acceptance criteria

Interpretation endpoint returns validated decisions or an explicit clarification/fallback; no free generation reaches the client; measured latency recorded.

## Log

- [2026-09-22 04:51 UTC] Ticket created.
- [2026-09-22 05:05 UTC] Jev interpreter
- [2026-09-22 05:05 UTC] worker/src/jev.ts: one call with 11 closed-set questions (company, compare_to, view, topic, layer, two cohort dimensions, event, timeframe, wants_testimony, needs_clarification), closed-set validation, fork extraction from probability spread, 7s timeout, deterministic fallback. Measured 126ms median.
- [2026-09-22 21:11 UTC] REOPENED: Audit C-01, C-02, C-03 and C-M1 (high) contradicted this acceptance: guessed fallbacks, low confidence applied instead of asking, and closed-set company selection that could silently pick the current page. The recorded latency (126 ms median) was for an earlier prompt version.
- [2026-09-22 21:11 UTC] Re-verified in round 3. Validated decisions or an explicit ask or labeled fallback: tests/jev.test.ts 'evaluate fails closed on unknown enums…', 'interpret fails closed instead of returning a guessed fallback', 'low-confidence primary readings are never applied…', 'a named employer we do not list asks…', 'provider failure returns a labeled degraded canvas that keeps the last view…' and the four 'live-probe regression (D8…)' tests pass (node --test "tests/*.test.ts" on 2026-09-22 21:00 UTC: 411 of 413 passed (the 2 failures were legal-page tests under edit by the legal group)). Latency measured for the current prompt: docs/evaluation.md: a live run of node tools/eval-intents.mjs by the Jev group on 2026-09-22 (TypeSafe API, jev-1.13.0, intent prompt v5, 27 cases run 3 times): 65 of 81 calls fully correct, route 79 of 79, p50 178 ms and p95 345 ms, thresholds imported from the code, 5 misses and 2 validation errors recorded. The harness measures the TypeSafe path; the native Workers AI path that production serves first is tracked in W-035 and W-044. Dependency corrected in round 3: W-006 needs the build and the local inference configuration (W-001), not W-002.
- [2026-09-22 21:41 UTC] Correction to the 21:11 entry: its evaluation figures (27 cases run 3 times, 65 of 81 calls fully correct, route 79 of 79, p50 178 ms and p95 345 ms) came from the first three runs and are not what the cited source says. The latency measured for the current prompt is p50 169 ms and p95 297 ms. docs/evaluation.md (re-read at 21:41 UTC; last changed at 20:58 UTC) states: 27 labeled cases run 4 times (108 calls) on the TypeSafe HTTP API, jev-1.13.0, intent prompt v5; 88 of 108 calls fully correct; route 106 of 106 answered calls; 2 validation errors (invalid_model_response); p50 169 ms and p95 297 ms pooled over the 106 answered calls (provider round trip from a development machine); 5 recorded misses. It measures the TypeSafe path only, not the Workers AI binding that production tries first.
- [2026-09-22 22:30 UTC] Correction to the 21:41 entry. docs/evaluation.md was rewritten at 21:48 UTC, after the 21:41 entry, so the figures that entry quotes are no longer its latest result (red-team finding RT-E1). Re-read for this entry, it states: 33 labeled cases run 4 times (132 calls) on the TypeSafe HTTP API, requested jev-latest and served jev-1.13.0, intent prompt shouldiworkthere-intent-v5, over the 224-employer directory; 111 of 132 cases correct (84%); 1 validation error (invalid_model_response, 'charlesschwab', fourth run); per-field rates over the calls whose case labels the field: employer 119 of 119, route 79 of 79, view 63 of 75, topic 64 of 68, case-specific checks 59 of 63; p50 193 ms and p95 269 ms pooled over the 131 answered calls (provider round trip from a development machine, not end to end). The 88 of 108, 2 errors, route 106 of 106 and p50 169 ms / p95 297 ms quoted at 21:41 are now given only as the same day's runs before the round-3 fixes (12-employer directory), rescored there with labeled-only denominators (route 27 of 27, not 106 of 106). The doc still measures only the TypeSafe path, not the Workers AI binding production tries first. The latency measured for the current prompt and directory is therefore p50 193 ms and p95 269 ms, not 169 ms and 297 ms. The criteria this ticket closed on still hold: the tests/jev.test.ts fail-closed, low-confidence and live-probe regression (D8) tests passed in node --test "tests/*.test.ts" at 22:28 UTC. [read docs/evaluation.md sha256:e191adc39c3f]
- [2026-09-23 01:26 UTC] Correction to the 22:30 entry. docs/evaluation.md was rewritten again at 23:01 UTC, after that entry, so the figures it quotes as the latest result (33 cases, 111 of 132, p50 193 ms and p95 269 ms) are now the doc's 'Earlier on the same day (round-3 fixer)' result. Re-read for this entry, its latest result (round-3 hardening) is: 37 labeled cases (the 33 plus 4 red-team cases) run 4 times, 148 calls, TypeSafe HTTP API, served jev-1.13.0, intent prompt shouldiworkthere-intent-v5 (unchanged), 224-employer directory; 128 of 148 correct (86%); 2 validation errors (invalid_model_response); labeled field rates employer 134 of 134, route 88 of 88, view 72 of 82, topic 63 of 67, case-specific checks 64 of 68, answer checks 15 of 15; p50 219 ms and p95 788 ms pooled over the 146 answered calls (the second batch had slower provider round trips: per-run p50/p95 192/263 and 196/245 ms, then 347/780 and 360/1168 ms). It still measures the TypeSafe path only.
  The doc predates the pre-deploy pass (after 23:26 UTC), whose interpreter changes the harness has not re-measured: the single-documented-event rule labels its event as inferred whenever it applies and asks when the question names a different year from the event's label; a published group named by its whole label applies from 0.45; a meaning fork is asked first when another field must be asked, with no view question beside it; other forks list only options at 0.12 or more, and company forks only employers the question plausibly names. These are covered by tests/jev.test.ts 'e2e regression (D8c)…', 'e2e regression: a published group the question names by its label…', 'D8d: when another field must be asked…' and 'fork noise (round-3 verification, failure 4)…'. Group A's polish report gives 7 of 7 for the D8d and round-3 verification eval cases on live Jev; the doc does not record that run. The criteria this ticket closed on still hold. Runs for this entry (group E, 2026-09-23 01:10–01:16 UTC, local three-worker stack with the local build): npx tsc --noEmit clean; node --test "tests/*.test.ts" 511 of 512 passed (the one failure was tests/tickets.test.ts, on the stale citations this sync settles); npx playwright test tests/browser.spec.ts 53 passed; npx playwright test tests/browser-contribute.spec.ts 53 passed, 1 skipped (the opt-in screenshot pass); node tests/integration.mjs 106 passed, 0 failed. [read docs/evaluation.md sha256:94776266f9a2]
