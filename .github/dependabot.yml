version: 2
updates:
  # Bun 1.4 lockfile v2 is not supported by Dependabot's parser yet (issue #8).
  # CI runs bun audit on the locked packages; update packages with Bun and review notices.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
