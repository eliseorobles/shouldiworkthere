# Optional: hosted Jev inference for local development.
# Without a key, deterministic controls and the fictional fixtures still work.
# prepare:local reads this value from .env or your shell and puts it only in
# .dev.vars.inference. Local cryptographic secrets are generated automatically.
TYPESAFE_API_KEY=
